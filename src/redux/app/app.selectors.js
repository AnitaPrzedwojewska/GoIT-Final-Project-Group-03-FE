export const getDarkMode = (state) => state.app.darkMode;

export const getSlideMenu = (state) => state.app.slideMenu;

export const getModalUser = (state) => state.app.modalUser;

export const getModalUserInfo = (state) => state.app.modalUserInfo;

export const getModalUserLogout = (state) => state.app.modalUserLogout;

export const getModalUserAvatar = (state) => state.app.modalUserAvatar;

export const getIsError = (state) => state.app.isError;
